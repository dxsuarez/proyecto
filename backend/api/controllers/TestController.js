module.exports = {
  /*
  subscribe: function(req, res) {
    sails.log.error('----------------------------------------------------------------');
    sails.log.error(req);
    sails.log.error(res);
    if( ! req.isSocket) {
      return res.badRequest();
    }

    sails.log.error('TestController, subscribe');

    sails.sockets.join(req.socket, 'test');

    return res.ok();
  }
  */
};

